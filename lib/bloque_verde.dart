/// Determina si [numero] puede insertarse en [numeros] (los números
/// ya colocados en un bloque VERDE).
///
/// Reglas del bloque verde: puede recibir cualquier número, sin
/// ninguna restricción. Siempre retorna `true`.
///
/// Se mantiene la firma (List<int>, int) -> bool igual a la de los
/// demás bloques, aunque [numeros] no se use dentro de la función.
bool insertarBloqueVerde(List<int> numeros, int numero) {
  return true;
}
 